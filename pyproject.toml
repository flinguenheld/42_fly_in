[project]
name = "42-fly-in"
version = "0.1.0"
description = "Drones are interesting."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.19.1",
    "textual>=8.1.1",
    "textual-canvas>=1.1.0",
]
